## Testing note

These rules are designed to run inside a SailPoint IdentityIQ instance (v8.x).  
To test locally: import via Setup > Rules, attach to an HR CSV source,  
and trigger an aggregation with the sample data in `/data/hr-sample.csv`.

A local IIQ instance requires: Tomcat 9, MySQL 8, IIQ developer license.
